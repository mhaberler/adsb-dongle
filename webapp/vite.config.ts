import { defineConfig } from "vite";

// Served from https://<user>.github.io/adsb-dongle/ (GitHub Pages project
// site) — asset URLs need the repo name as a base path.
export default defineConfig({
  base: "/adsb-dongle/",
});
