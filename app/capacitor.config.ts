import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.mhaberler.adsbdongle",
  appName: "ADS-B Dongle",
  webDir: "dist",
};

export default config;
